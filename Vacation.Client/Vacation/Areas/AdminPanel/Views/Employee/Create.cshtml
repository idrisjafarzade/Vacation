@*@using Vacation.Areas.AdminPanel.ViewModels
@{
    ViewData["Title"] = "Create";
}
@model EmpolyeeVM

<div class="container">
<div class="row">
    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
        <div class="card-body">
            <h4 class="card-title">Create Speaker</h4>
            <form class="forms-sample" method="post" enctype="multipart/form-data">
                <div class="form-group">
                    <label asp-for="@Model.employee.Fullname ">Employee Fullanme</label>
                    <input asp-for="@Model.employee.Fullname " class="form-control" placeholder="Fullname">
                    <span class="text-danger" asp-validation-for="@Model.employee.Fullname "></span>
                </div>
                   <div class="form-group">
                    <label asp-for="@Model.employee.Position ">Position</label>
                    <input asp-for="@Model.employee.Position " class="form-control" placeholder="Position">
                    <span class="text-danger" asp-validation-for="@Model.employee.Position "></span>
                 </div>
                <div class="form-group" >
                    <label asp-for="@Model.Vacation">Category</label>
                    <span class="text-danger" asp-validation-for="@Model.Vacation"></span>
                    <select class="form-control" name="CategoryId">
                        <option value="0">--Select Category--</option>
                        @foreach (var item in Model.Vacation)
                        {
                            <option value="@item.Id">@item.Name</option>
                        }
                    </select>
                </div>
                <button type="submit" class="btn btn-primary mr-2 ">Submit</button>
                <a class="btn btn-light" asp-action="Index">Cancel</a>
            </form>
        </div>
        </div>
    </div>
</div>
</div>





*@